import LoginClient from './LoginClient'

export const metadata = {
  title: 'Login - Saviya',
  description: 'Sign in to access your secure B2B escrow workspace.',
}

export default function LoginPage() {
  return <LoginClient />
}
